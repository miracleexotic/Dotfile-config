---
- name: Installation for My environment configuration workspace.
  hosts: server
  connection: local
  gather_facts: false
  become: true
  vars:
    user: "user"
  environment:
    GNUTLS_CPUID_OVERRIDE: 0x1

  tasks:
    - include: ./bash/install.yaml
    - include: ./zsh/install.yaml
    - include: ./starship/install.yaml
    - include: ./tmux/install.yaml
